{#
/**
 * @file
 * Theme override to display a single page.
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template in this directory.
 *
 * Available variables:
 *
 * General utility variables:
 * - base_path: The base URL path of the Drupal installation. Will usually be
 *   "/" unless you have installed Drupal in a sub-directory.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and signed in.
 * - is_admin: A flag indicating if the user has permission to access
 *   administration pages.
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page. This includes the language domain or prefix.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - messages: Status and error messages. Should be displayed prominently.
 * - node: Fully loaded node, if there is an automatically-loaded node
 *   associated with the page and the node ID is the second argument in the
 *   page's path (e.g. node/12345 and node/12345/revisions, but not
 *   comment/reply/12345).
 *
 * Regions:
 * - page.header: Items for the header region.
 * - page.primary_menu: Items for the primary menu region.
 * - page.secondary_menu: Items for the secondary menu region.
 * - page.highlighted: Items for the highlighted content region.
 * - page.help: Dynamic help text, mostly for admin pages.
 * - page.content: The main content of the current page.
 * - page.sidebar_first: Items for the first sidebar.
 * - page.sidebar_second: Items for the second sidebar.
 * - page.footer: Items for the footer region.
 * - page.breadcrumb: Items for the breadcrumb region.
 *
 * @see template_preprocess_page()
 * @see html.html.twig
 */
#}
{% set portfolio_layout_temp = 'layout'|get_path_param %}
{% if portfolio_layout_temp %}
    {% set portfolio_layout = portfolio_layout_temp %}
{% endif %}
{% set portfolio_style_temp = 'style'|get_path_param %}
{% if portfolio_style_temp %}
    {% set portfolio_style = portfolio_style_temp %}
{% endif %}
{% if portfolio_layout is empty %}
    {% set portfolio_layout = 'boxed' %}
{% endif %}
{% if portfolio_style is empty %}
    {% set portfolio_style = 'gutter2cols' %}
{% endif %}
{% if portfolio_menu_style is empty %}
    {% set menu_style = portfolio_menu_style %}
{% endif %}
{% if portfolio_footer_style is not empty %}
    {% set footer_style = portfolio_footer_style %}
{% endif %}
{% if portfolio_slogan is not empty %}
    {% set page_slogan = portfolio_slogan %}
{% endif %}
{% if portfolio_page_header_class is not empty %}
    {% set page_header_class = portfolio_page_header_class %}
{% endif %}
{% if portfolio_page_header_bgcolor is not empty %}
    {% set page_header_bgcolor = portfolio_page_header_bgcolor %}
{% endif %}
{% if portfolio_page_title_bgimage is not empty %}
    {% set page_title_bgimage = portfolio_page_title_bgimage %}
{% endif %}

<header>
    <!-- START NAV MENU -->
    <nav class="navbar-fixed-top {{ nav_class }}">
        {% if page.top_menu %}
            {{ page.top_menu }}
        {% endif %}
        <div class="w-main-menu">
            <div id="main-menu" class="container p-mob-none-left p-mob-none-right">
                {% if logo %}
                    <a href="{{ front_page }}" class="branding"
                       {% if fixed_logo %}data-fixed-logo="{{ fixed_logo }}"{% endif %}>
                        <img class="changeable" src="{{ logo }}" alt="{{ 'Home'|t }}">
                    </a>
                {% endif %}
                {% if page.main_menu %}
                    {{ page.main_menu }}
                {% endif %}
                <div class="right-mp-menu hide">
                    <ul>
                        {% if page.shopping_cart %}
                            <li class="cart">
                                <a href="#" class="js-stop-click"><i class="fa fa-shopping-cart"></i>{{ 'CART'|t }}
                                    (<span id="cart-items">0</span>)</a>
                                {{ page.shopping_cart }}
                            </li>
                        {% endif %}
                        <li><a id="filter-search" href="#" class="js-stop-click"><i
                                        class="fa fa-search"></i>{{ 'SEARCH'|t }}</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>
    <!-- END NAV BAR -->
    <!-- START PAGE HEADER -->
    {% if page_title_bgimage is not empty %}
        {% set page_header_class = page_header_class ~ ' navi-bg-image' %}
    {% elseif page_header_bgcolor is not empty %}
        {% set page_header_class = page_header_class ~ ' bg-color' %}
    {% endif %}
    {% if page_header_enable == 'on' %}
        <div class="page-header {{ page_header_class }}"
             {% if page_title_bgimage is not empty %}data-bg-image="{{ page_title_bgimage }}"{% endif %} {% if page_header_bgcolor is not empty %}data-bg-color="#{{ page_header_bgcolor }}"{% endif %}>
            <div class="container">
                <div class="row">
                    <div class="col-md-7">
                        <div class="page-header-title">
                            <h2>{{ current_page_title }}</h2>
                            <p>{{ page_slogan }}</p>
                        </div>
                    </div>
                    <div class="col-md-5">
                        <!-- START BREADCUMB -->
                        {% if page.breadcrumb %}
                            <div class="page-header-breadcrumb">
                                {{ page.breadcrumb }}
                            </div>
                        {% endif %}
                        <!-- END BREADCUMB -->
                    </div>
                </div>
            </div>
        </div>
        <!-- END PAGE HEADER -->
    {% endif %}
    <!-- END NAV BAR -->
</header>

{% if page.site_feedback %}
    {{ page.site_feedback }}
{% endif %}
{{ messages }}
{% if page.before_content %}
    {{ page.before_content }}
{% endif %}


<div id='content-area' class="container">
{% if page.content and node.field_images is not defined %}
    <div class="portfolio landing-page-padding-title">
        <div class="container{% if portfolio_layout == 'wide' %}-fluid{% endif %}">
            <div class="row">
                <div class="col-md-12">
                    <div class="wb-grid">
                        <div class="wb-center">
                            <div class="grid">
                                {{ page.content }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% elseif page.content and node.field_images is defined %}
    <div class="portfolio-single-area">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="single_portfolio_title">
                        <h2>{{ current_page_title }}</h2>
                    </div>
                </div>
            </div>
        </div>
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    {% if node.field_images|length > 1 %}
                        <div id="single-portfolio-slider">
                            {% for img in node.field_images %}
                                <div class="item"><img src="{{ img.entity.fileuri|image_style_url('image_1140x760') }}"
                                                       alt="{{ img.alt }}"></div>
                            {% endfor %}
                        </div>
                    {% else %}
                        <div class="single-image"><img
                                    src="{{ node.field_images.0.entity.fileuri|image_style_url('image_1140x760') }}"
                                    alt="{{ node.field_images.0.alt }}"></div>
                    {% endif %}
                </div>
            </div>
        </div>
        <div class="project-details">
            <div class="container">
                <div class="row">
                    {{ page.content }}
                </div>
            </div>
        </div>
    </div>
{% endif %}
</div>

{% if page.after_content %}
    {{ page.after_content }}
{% endif %}


<footer id="footer" class="footer clearfix">
    {% if page.footer_first and page.footer_second and page.footer_third and page.footer_fourth %}
        <div id="footer-main" class="footer-forth container">
            <div class="footer-sub col-md-3 col-sm-6 p-none-left">
                {{ page.footer_first }}
            </div>
            <div class="footer-sub col-md-3 col-sm-6">
                {{ page.footer_second }}
            </div>
            <div class="footer-sub col-md-3 col-sm-6">
                {{ page.footer_third }}
            </div>
            <div class="footer-sub col-md-3 col-sm-6 ">
                {{ page.footer_fourth }}
            </div>
        </div>
    {% elseif page.footer_first and page.footer_second and page.footer_third %}
        <div id="footer-main"  class="footer-third container">
            <div class="footer-sub col-md-4 col-sm-6">
                {{ page.footer_first }}
            </div>
            <div class="footer-sub col-md-4 col-sm-6">
                {{ page.footer_second }}
            </div>
            <div class="footer-sub col-md-4 col-sm-6">
                {{ page.footer_third }}
            </div>
        </div>
    {% elseif page.footer_first and page.footer_second %}
        <div id="footer-main" class="footer-second container ">
            <div class="footer-sub col-md-6 col-sm-6">
                {{ page.footer_first }}
            </div>
            <div class="footer-sub col-md-6 col-sm-6">
                {{ page.footer_second }}
            </div>
        </div>
    {% elseif page.footer_first %}
        <div id="footer-main" class="footer-first container">
            <div class="footer-sub  col-md-4 col-sm-6">
                {{ page.footer_first }}
            </div>
        </div>
    {% endif %}
    {% if page.footer_copyright %}
        <div id="footer-copyright">
            <div class="container">
                {{ page.footer_copyright }}
            </div>
        </div>
    {% endif %}
</footer>
